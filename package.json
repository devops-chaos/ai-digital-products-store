{
  "name": "ai-digital-products-store",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered digital products marketplace with OpenAI and Stripe integration points.",
  "scripts": {
    "start": "node server.js",
    "serve": "node server.js",
    "static": "python -m http.server 4173",
    "check": "node --check server.js && node --check assets/site.js && node --check assets/catalog.js && node --check api/ai-recommend.js && node --check api/create-checkout-session.js && node --check api/generate-prompt.js && node --check api/health.js && node --check api/stripe-webhook.js && node --check api/support-request.js && node --check lib/commerce-ai.js"
  },
  "dependencies": {
    "openai": "^6.0.0",
    "stripe": "^18.0.0"
  },
  "devDependencies": {}
}
